---
interface Props {
  id?: string;
  eyebrow: string;
  title: string;
  description: string;
}

const { id, eyebrow, title, description } = Astro.props;
---

<article {id} class="editorial-card">
  <p class="eyebrow">{eyebrow}</p>
  <h3>{title}</h3>
  <p>{description}</p>
  <slot />
</article>
